# IoT Data Server Environment Variables
# Copy this file to .env and fill in your values

# Admin credentials
ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD=your_admin_password

# Security
SECRET_KEY=your_secret_key_for_session_management

# Database configuration
# SQLite is used by default, no additional configuration needed
# If you want to use a different database, uncomment and configure the following:
# DATABASE_URL=your_database_url

# MQTT Broker settings (if using a custom broker)
# MQTT_BROKER=localhost
# MQTT_PORT=1883
# MQTT_USERNAME=mqtt_username
# MQTT_PASSWORD=mqtt_password
